mod systems {
    mod start; 
    mod spawn;
    mod actions;
    mod mapmaker;
}

mod models {
    mod bullet;
    mod sessions;
    mod map;
    mod characters;
    mod global;
    mod player;
    mod turndata;
}

mod lib {
    mod default_spawns;
    mod helpers;
    mod simulate;
    mod trig;
}

mod tests {
    mod helpers;
  //  mod test_world;
}

mod consts;
mod types;
